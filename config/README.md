# Configuration

Non-secret configuration templates and configuration documentation only.

Secrets belong in `.env`, which is git-ignored. See `.env.example` at the repository root for the full list of variables.
